#!/usr/bin/env perl
#
# use product_deps

use File::Basename;
use lib dirname($0);

if( $#ARGV >= 0 ) {
   $inputdir = $ARGV[0];
} else {
    print "\n";
    print "ERROR: directory not specified\n";
    print "USAGE: report_libdir <input-directory>\n";
    print "       input-directory  = directory containing product_deps\n";
    print "\n";
    exit 1;
}

require parse_deps;

$pfile=$inputdir."/product_deps";
$lib_directory = get_cmake_lib_directory( $pfile );
print "$lib_directory\n";

exit 0;
